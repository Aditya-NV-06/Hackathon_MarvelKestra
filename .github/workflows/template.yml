name: CI

on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
        - name: issue_comment
          uses: ./Templates/issue_template.yml
        
        - name: pr_template
          uses: ./Templates/pr_template.yml

     